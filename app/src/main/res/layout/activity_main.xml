<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">


    <View
        android:id="@+id/top_menu_view"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:background="@color/md_theme_primary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />


    <ImageView
        android:id="@+id/search_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_search"
        app:layout_constraintBottom_toBottomOf="@+id/top_menu_view"
        app:layout_constraintEnd_toStartOf="@id/favorite_icon"
        app:layout_constraintStart_toStartOf="@+id/top_menu_view"
        app:layout_constraintTop_toTopOf="@+id/top_menu_view"
        app:tint="@color/md_theme_onPrimary" />

    <ImageView
        android:id="@+id/favorite_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"

        android:src="@drawable/ic_favorite"
        app:layout_constraintBottom_toBottomOf="@+id/top_menu_view"
        app:layout_constraintEnd_toStartOf="@+id/list_icon"
        app:layout_constraintStart_toEndOf="@id/search_icon"
        app:layout_constraintTop_toTopOf="@+id/top_menu_view"
        app:tint="@color/md_theme_onPrimary"/>
    <ImageView
        android:id="@+id/list_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"

        android:src="@drawable/ic_list"
        app:layout_constraintBottom_toBottomOf="@+id/top_menu_view"
        app:layout_constraintEnd_toEndOf="@+id/top_menu_view"
        app:layout_constraintStart_toEndOf="@id/favorite_icon"
        app:layout_constraintTop_toTopOf="@+id/top_menu_view"
        app:tint="@color/md_theme_onPrimary"/>

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/main_fragment_container_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/top_menu_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>